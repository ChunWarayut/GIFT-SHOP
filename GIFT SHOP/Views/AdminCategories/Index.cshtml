@model IEnumerable<GIFT_SHOP.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("เพิ่มข้อมูล", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ca_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ca_name)
        </td>
        <td>
            @Html.ActionLink("แก้ไข", "Edit", new { id=item.Ca_ID }) |
            @Html.ActionLink("ลบ", "Delete", new { id=item.Ca_ID })
        </td>
    </tr>
}

</table>
